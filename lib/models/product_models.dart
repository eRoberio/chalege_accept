
class ProductData {
  String titulo;
  String descricao;
  bool excluido;
  bool feito;
  DateTime data;

  ProductData(
      {this.titulo, this.descricao, this.excluido, this.feito, this.data});
}
